import{EM,B,NMO,SQ,GREEN,RED,BLUE,YELLOW,R,A,Y,G,arrayToGrid,Sticker}from"./utils.js";let colors;const HRR=1,VRR=2,WHITE=16777215,GLOST=1,HAS_RANDOM_SOLUTION=2,WILL_TRY_LATER=3,CNF=4;class SA{constructor(cols,rows){this.cols=cols,this.rows=rows,colors=[YELLOW,GREEN,RED,BLUE]}SBO(grid,tile){if(1===tile.count)return void(tile.gelost=!0);const{verticals:verticals,horizontals:horizontals}=this.possBO(grid,tile.col,tile.row,tile.count,tile),horPaths=this.getHorizontalPaths(horizontals,tile.count,tile.fillColor,grid),verPaths=this.getVerticalPaths(verticals,tile.count,tile.fillColor,grid),VL=verPaths.length,HL=horPaths.length,hos=1===VL&&0===HL||1===HL&&0===VL;if(!0===hos){const locs=1===VL?verPaths[0]:horPaths[0];for(let loc of locs){const{col:col,row:row}=loc,sq=grid[col][row];sq.og={col:tile.col,row:tile.row},sq.setFillStyle(tile.fillColor),sq.gelost=!0}return 1}return{verPaths:verPaths,horPaths:horPaths}}SNO(grid,tile){if(1===tile.count)return this.solveLoneEM(grid,tile);const{col:col,row:row,count:count}=tile,{vers:vers,hors:hors}=this.getPossiblesNMO(grid,col,row,count,tile),horPaths=this.getHorPathsNMO(hors,tile.count,grid),verPaths=this.getVerPathsNMO(vers,tile.count,grid),horOKs=this.filterHorizontals(horPaths,grid),verOKs=this.filterVerticals(verPaths,grid),HL=horOKs.length,VL=verOKs.length,hos=1===VL&&0===HL||1===HL&&0===VL;if(hos){const arr=0===VL?horOKs[0]:verOKs[0];let color=this.findColor(arr,grid);if(color!==WHITE){for(let locs of arr){const{col:col,row:row}=locs,sq=grid[col][row];sq.setFillStyle(color),sq.og={col:tile.col,row:tile.row},sq.gelost=!0}return 1}if(color=this.eliminateColors(arr,grid),void 0!==color){for(let locs of arr){const{col:col,row:row}=locs,sq=grid[col][row];sq.setFillStyle(color),sq.og={col:tile.col,row:tile.row},sq.gelost=!0}return 1}return 4}return{horOKs:horOKs,verOKs:verOKs}}solveLoneEM(grid,tile){if(tile.gelost)return 1;let ns=this.getNeighbors(tile.col,tile.row);if(ns=ns.map(item=>grid[item.col][item.row]),tile.hint===EM){const areAllSafe=ns.every((item,i,arr)=>{if(item.gelost)return!0;if((item.hint===B||item.hint===NMO)&&1===item.count)return!0;const dir=item.col===tile.col?2:1;if(item.hint===B||item.hint===SQ){if(1===dir){const topIsClear=this.topIsClear(item.col,item.row,item.fillColor,grid);return!1===topIsClear}{const sideIsClear=this.sideIsClear(item.col,item.row,item.fillColor,grid);return!1===sideIsClear}}return!1});if(!1===areAllSafe)return 3;tile.count=1,tile.hint=NMO}let cors=ns.map(item=>item.fillColor),canFit=0,num;for(let cor of colors)-1===cors.indexOf(cor)&&!0===this.stickerIsLegal(tile.col,tile.row,cor,grid)&&canFit++;if(canFit>=2)return 2;for(let i=0;i<colors.length;i++)if(-1===cors.indexOf(colors[i])){num=i;break}return tile.setFillStyle(colors[num]),tile.gelost=!0,this.count=1,1}getPossiblesNMO(grid,col,row,count,tile){let hors=[];for(let i=col-(count-1);i<=col;i++)this.isWithinLimit(i,row)&&hors.push({col:i,row:row});hors=hors.filter(item=>{const c=item.col,r=item.row,poss=grid[c][r];return poss.hint!==B&&poss.hint!==NMO||poss.count===count});let vers=[];for(let i=row-(count-1);i<=row;i++)this.isWithinLimit(col,i)&&vers.push({col:col,row:i});return vers=vers.filter(item=>{const c=item.col,r=item.row,poss=grid[c][r];return poss.hint!==NMO&&poss.hint!==B||poss.count===count}),{vers:vers,hors:hors}}getHorPathsNMO(list,count,grid){let paths=[];for(let locs of list){let{col:col,row:row}=locs,n=0,cors=[],arr=[];for(;n<count&&this.isWithinLimit(col,row);){const sq=grid[col][row];if(sq.gelost)break;if((sq.hint===B||sq.hint===NMO)&&sq.count!==count)break;arr.push({col:col,row:row}),cors.push(sq.fillColor),col++,n++}const hasOnlyOneColor=this.hasOnlyOneColor(cors);cors.length===count&&!0===hasOnlyOneColor&&paths.push(arr)}return paths}getVerPathsNMO(list,count,grid){let paths=[];for(let locs of list){let{col:col,row:row}=locs,n=0,cors=[],arr=[];for(;n<count&&this.isWithinLimit(col,row);){const sq=grid[col][row];if(sq.gelost)break;if((sq.hint===B||sq.hint===NMO)&&sq.count!==count)break;arr.push({col:col,row:row}),cors.push(sq.fillColor),row++,n++}const hasOnlyOneColor=this.hasOnlyOneColor(cors);cors.length===count&&!0===hasOnlyOneColor&&paths.push(arr)}return paths}hasOnlyOneColor(arr){let prev=void 0;for(let chaque of arr)if(chaque!==WHITE){if(void 0!==prev&&prev!==chaque)return!1;prev=chaque}return!0}filterHorizontals(paths,grid){let lists=[];for(let p of paths){let c=0;const color=this.findColor(p,grid),isClear=this.endsAreClear(1,p,color,grid);if(!1!==isClear){sec:for(let locs of p){const{col:col,row:row}=locs;if(!1===this.topIsClear(col,row,color,grid))break sec;c++}c===p.length&&lists.push(p)}}return lists}filterVerticals(paths,grid){let lists=[];for(let p of paths){let c=0;const color=this.findColor(p,grid),isClear=this.endsAreClear(2,p,color,grid);if(!1!==isClear){sec:for(let locs of p){const{col:col,row:row}=locs;if(!1===this.sideIsClear(col,row,color,grid))break sec;c++}c===p.length&&lists.push(p)}}return lists}findColor(arr,grid){let color;for(let c of arr){const{col:col,row:row}=c,sq=grid[col][row];if(sq.fillColor!==WHITE)return color=sq.fillColor,color}return WHITE}possBO(grid,col,row,count,tile){const color=tile.fillColor;let hors=[];for(let i=col-(count-1);i<=col;i++)this.isWithinLimit(i,row)&&hors.push({col:i,row:row});hors=hors.filter(item=>{const c=item.col,r=item.row,poss=grid[c][r];if((poss.hint===NMO||poss.hint===B)&&poss.count!==count)return!1;const colorIsOK=poss.fillColor===WHITE||poss.fillColor===color;return colorIsOK&&this.topIsClear(c,r,color,grid)});let vers=[];for(let i=row-(count-1);i<=row;i++)this.isWithinLimit(col,i)&&vers.push({col:col,row:i});return vers=vers.filter(item=>{const c=item.col,r=item.row,poss=grid[c][r];if((poss.hint===NMO||poss.hint===B)&&poss.count!==count)return!1;const colorIsOK=poss.fillColor===WHITE||poss.fillColor===color;return colorIsOK&&this.sideIsClear(c,r,color,grid)}),{verticals:vers,horizontals:hors}}topIsClear(col,row,color,grid){if(color===WHITE)return!0;const top={col:col,row:row-1},bot={col:col,row:row+1};let locs=[top,bot];locs=locs.filter(item=>this.isWithinLimit(item.col,item.row));for(let loc of locs){const c=loc.col,r=loc.row,sq=grid[c][r];if(sq.fillColor===color)return!1}return!0}sideIsClear(col,row,color,grid){if(color===WHITE)return!0;const left={col:col-1,row:row},right={col:col+1,row:row};let locs=[left,right];locs=locs.filter(item=>this.isWithinLimit(item.col,item.row));for(let loc of locs){const c=loc.col,r=loc.row,sq=grid[c][r];if(sq.fillColor===color)return!1}return!0}isWithinLimit(col,row){return col>=0&&col<this.cols&&row>=0&&row<this.rows}getNeighbors(col,row){let locs=[];const up={col:col,row:row-1},right={col:col+1,row:row},down={col:col,row:row+1},left={col:col-1,row:row};return locs.push(up,right,down,left),locs=locs.filter(item=>item.col>=0&&item.col<this.cols&&item.row>=0&&item.row<this.rows),locs}getHorizontalPaths(list,count,color,grid){let paths=[];for(let item of list){let n=0,arr=[],{col:col,row:row}=item;for(;n<count&&this.isWithinLimit(col,row);){const sq=grid[col][row];if((sq.hint===NMO||sq.hint===B)&&sq.count!==count)break;if(sq.fillColor!==WHITE&&sq.fillColor!==color||!0===sq.gelost)break;if(!1===this.topIsClear(col,row,color,grid))break;arr.push({col:col,row:row}),col++,n++}if(0!==arr.length){const endsAreClear=this.endsAreClear(1,arr,color,grid);arr.length===count&&endsAreClear&&paths.push(arr)}}return paths}getVerticalPaths(list,count,color,grid){let paths=[];for(let item of list){let n=0,arr=[],{col:col,row:row}=item;for(;n<count&&this.isWithinLimit(col,row);){const sq=grid[col][row];if((sq.hint===NMO||sq.hint===B)&&sq.count!==count)break;if(sq.fillColor!==WHITE&&sq.fillColor!==color||!0===sq.gelost)break;if(!1===this.sideIsClear(col,row,color,grid))break;arr.push({col:col,row:row}),row++,n++}if(0!==arr.length){const endsAreClear=this.endsAreClear(2,arr,color,grid);arr.length===count&&endsAreClear&&paths.push(arr)}}return paths}endsAreClear(dir,arr,color,grid){if(color===WHITE)return!0;if(1===dir){const start=arr[0],end=arr[arr.length-1],first={col:start.col-1,row:start.row},last={col:end.col+1,row:end.row};let locs=[first,last],clearCount=0;for(let loc of locs){const isWithin=this.isWithinLimit(loc.col,loc.row);if(!1===isWithin){clearCount++;continue}const sq=grid[loc.col][loc.row];sq.fillColor!==WHITE&&sq.fillColor===color||clearCount++}return 2===clearCount}if(2===dir){const start=arr[0],end=arr[arr.length-1],first={col:start.col,row:start.row-1},last={col:end.col,row:end.row+1};let locs=[first,last],clearCount=0;for(let loc of locs){const isWithin=this.isWithinLimit(loc.col,loc.row);if(!1===isWithin){clearCount++;continue}const sq=grid[loc.col][loc.row];sq.fillColor!==WHITE&&sq.fillColor===color||clearCount++}return 2===clearCount}}findFirstRelative(tile,grid){let ns=this.getNeighbors(tile.col,tile.row),rels=[];if(ns.forEach(item=>{const sq=grid[item.col][item.row];sq.fillColor===tile.fillColor&&rels.push(sq)}),0!==rels.length){if(1===rels.length){const dir=tile.col===rels[0].col?2:1,rela=rels[0];if(1===dir){const first=tile.col<rela.col?tile:rela;return first.count=tile.count,first}{const first=tile.row<rela.row?tile:rela;return first.count=tile.count,first}}if(2===rels.length){const r1=rels[0],r2=rels[1];rels.forEach(item=>{item.num.text=`${tile.count}`,item.num.setVisible(!0)});const dir=r1.col===r2.col?2:1;if(1===dir){const first=r1.col<r2.col?r1:r2;return first}{const first=r1.row<r2.row?r1:r2;return first}}}}isInList(x,y,list){for(let i=0;i<list.length;i++){const col=list[i].col,row=list[i].row;if(x===col&&y===row)return!0}return!1}lookForColor(arr,grid){const start=arr[0],end=arr[arr.length-1];let cors=[],{col:col,row:row}=start,ns=this.getNeighbors(col,row);if(ns=ns.filter(item=>{const{col:col,row:row}=item;return!1===this.isInList(col,row,arr)}),3===ns.length&&(ns.forEach(item=>{const sq=grid[item.col][item.row];sq.fillColor!==WHITE&&cors.push(sq.fillColor)}),3===cors.length)){const hasThree=this.hasThreeColors(arr);if(hasThree)return this.findThirdColor(cors)}if(col=end.col,row=end.row,ns=this.getNeighbors(col,row),ns=ns.filter(item=>{const{col:col,row:row}=item;return!1===this.isInList(col,row,arr)}),3===ns.length&&(ns.forEach(item=>{const sq=grid[item.col][item.row];sq.fillColor!==WHITE&&cors.push(sq.fillColor)}),3===cors.length)){const hasThree=this.hasThreeColors(arr);if(hasThree)return this.findThirdColor(cors)}}findThirdColor(cors){for(let i=0;i<colors.length;i++){const cor=colors[i];if(-1===cors.indexOf(cor))return colors[i]}}eliminateColors(arr,grid){let cors=[];const dir=arr[0].col===arr[1].col?2:1;for(let locs of arr){const{col:col,row:row}=locs;let p=[];p=1===dir?this.getTopColors(col,row,grid):this.getSideColors(col,row,grid);for(let color of p)-1===cors.indexOf(color)&&cors.push(color)}const endColors=this.getEndColors(arr,dir,grid);for(let cor of endColors)-1===cors.indexOf(cor)&&cors.push(cor);if(3!==cors.length)return;let num;for(let i=0;i<colors.length;i++)-1===cors.indexOf(colors[i])&&(num=i);return colors[num]}getEndColors(arr,dir,grid){if(1===dir){const start=arr[0],end=arr[arr.length-1],first={col:start.col-1,row:start.row},last={col:end.col+1,row:end.row};let locs=[first,last],cors=[];for(let loc of locs){const isWithin=this.isWithinLimit(loc.col,loc.row);if(!1===isWithin)continue;const sq=grid[loc.col][loc.row];sq.fillColor!==WHITE&&cors.push(sq.fillColor)}return cors}if(2===dir){const start=arr[0],end=arr[arr.length-1],first={col:start.col,row:start.row-1},last={col:end.col,row:end.row+1};let locs=[first,last],cors=[];for(let loc of locs){const isWithin=this.isWithinLimit(loc.col,loc.row);if(!1===isWithin)continue;const sq=grid[loc.col][loc.row];sq.fillColor!==WHITE&&cors.push(sq.fillColor)}return cors}}getTopColors(col,row,grid){const top={col:col,row:row-1},bot={col:col,row:row+1};let locs=[top,bot];return locs=locs.filter(item=>this.isWithinLimit(item.col,item.row)).map(item=>grid[item.col][item.row].fillColor),locs=locs.filter(item=>item!==WHITE),locs}getSideColors(col,row,grid){const left={col:col-1,row:row},right={col:col+1,row:row};let locs=[left,right];return locs=locs.filter(item=>this.isWithinLimit(item.col,item.row)).map(item=>grid[item.col][item.row].fillColor),locs=locs.filter(item=>item!==WHITE),locs}hasThreeColors(arr){let cors=[];arr.forEach(item=>{cors.push(item.fillColor)});const hasThree=cors.every((item,i,list)=>{const index=list.indexOf(item,i+1);return-1===index});return hasThree}appearsTwice(arr,elem){let count=0;for(let item of arr)item===elem&&count++;return 2===count}isLoneEM(grid,tile){if(tile.hint===NMO&&1===tile.count||!0===tile.gelost||tile.hint!==EM||tile.fillColor!==WHITE)return!1;let neighbors=this.getNeighbors(tile.col,tile.row);neighbors=neighbors.map(item=>grid[item.col][item.row]);const isLone=neighbors.every(item=>!0===item.gelost||(item.hint===NMO||item.hint===B)&&1===item.count||item.fillColor!==WHITE);return isLone}gridIsGLOST(grid){for(let i=0;i<this.cols;i++)for(let k=0;k<this.rows;k++){const sq=grid[i][k];if(sq.fillColor===WHITE)return!1}return!0}checkIfSorroundedByThreeDifferent(grid,tile){if(tile.hint===B||tile.hint===SQ||!0===tile.gelost)return!1;let ns=this.getNeighbors(tile.col,tile.row);ns=ns.map(item=>grid[item.col][item.row]);let assigned=0;if(ns.forEach(item=>{const isGLOST=!0===item.gelost;isGLOST&&assigned++}),assigned<3)return!1;let cors=[];if(ns.forEach(item=>{-1===cors.indexOf(item.fillColor)&&item.fillColor!==WHITE&&cors.push(item.fillColor)}),3!==cors.length)return!1;const color=this.findThirdColor(cors);tile.setFillStyle(color),tile.gelost=!0,tile.hint===EM?tile.hint=SQ:tile.hint===NMO&&(tile.hint=B)}countGLOST(grid){let GLOST=0;for(let i=0;i<this.cols;i++)for(let k=0;k<this.rows;k++){const sq=grid[i][k];grid[i][k].fillColor!==WHITE&&GLOST++}return GLOST}sA(grid){let runs=0,GLOST=0,oldGLOST,isGLOST=!1;for(;!1===isGLOST&&runs<5;){for(let i=0;i<this.cols;i++)for(let k=0;k<this.rows;k++){const tile=grid[i][k];void 0===tile.gelost&&(tile.hint===B?this.SBO(grid,tile):tile.hint===NMO&&this.SNO(grid,tile),!0===this.isLoneEM(grid,tile)&&this.solveLoneEM(grid,tile),this.checkIfLoneUnmarked(grid,tile))}isGLOST=this.gridIsGLOST(grid),oldGLOST=GLOST,GLOST=this.countGLOST(grid),oldGLOST===GLOST&&runs++}return isGLOST}checkIfLoneUnmarked(grid,tile){if(!0===tile.gelost||tile.hint!==SQ||tile.fillColor===WHITE)return;const list=this.getNeighbors(tile.col,tile.row),isLoneUnmarked=list.every(item=>{const{col:col,row:row}=item,sq=grid[col][row];return!0===sq.gelost||sq.fillColor!==WHITE&&sq.fillColor!==tile.fillColor||(sq.hint===NMO||sq.hint===B)&&1===sq.count});return!0===isLoneUnmarked&&(tile.count=1,tile.hint=B,tile.gelost=!0,!0)}stickerIsLegal(col,row,color,grid){if(16777215===color)return!0;let list=this.getNeighbors(col,row),objs=[];if(list.forEach(item=>{const obj=grid[item.col][item.row];objs.push(obj)}),objs=objs.filter(item=>item.fillColor===color),0===objs.length)return!0;let verTimes=0,horTimes=0;for(let i=0;i<objs.length;i++){const sq=objs[i],dir=sq.row===row?1:2;!1===this.wouldBeIllegal(dir,sq.fillColor,sq,grid)&&(1===dir?horTimes++:verTimes++)}const isIntersect=!(1!==verTimes&&2!==verTimes||1!==horTimes&&2!==horTimes),isClash=1===verTimes&&objs.length>1||2===verTimes&&objs.length>2||1===horTimes&&objs.length>1||2===horTimes&&objs.length>2,isIllegal=0===horTimes&&0===verTimes;return!1===isIntersect&&!1===isClash&&!1===isIllegal}wouldBeIllegal(dir,color,obj,grid){const{col:col,row:row}=obj;if(1===dir){let neighbors=this.getNeighbors(col,row);neighbors=neighbors.filter(item=>item.col===col);for(let i=0;i<neighbors.length;i++){const c=neighbors[i],sq=grid[c.col][c.row];if(sq.fillColor===color)return!0}}if(2===dir){let neighbors=this.getNeighbors(col,row);neighbors=neighbors.filter(item=>item.row===row);for(let i=0;i<neighbors.length;i++){const c=neighbors[i],sq=grid[c.col][c.row];if(sq.fillColor===color)return!0}}return!1}}export{SA};